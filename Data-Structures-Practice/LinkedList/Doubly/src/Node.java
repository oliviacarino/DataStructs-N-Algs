// Linked list Node.  This inner class is made static so that main() can a//ccess it
public class Node {
    int data;
    Node next;
    Node prev;
    Node(int d) {
        data = d;
//        next = null;
//        prev = null; // used for doubly linked list
    } // Constructor
} // Node
